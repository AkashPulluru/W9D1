class MovingObject {
    constructor(args) {
        this.pos = args[pos];
        this.vel = args[vel];
        this.radius = args[radius];
        this.color = args[color];
    }

    draw(ctx) {
        c
    }
    
    move() {
        this.pos += this.vel 
    }
}

export default MovingObject;